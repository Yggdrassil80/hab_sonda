#!/usr/bin/python

import os
import logging

#metodo que verifica si el comando es valido
def isValidCommand(command):
    return False

#metodo que escribe el comando en el path donde los servicios lo van a leer para su ejecucion
def writeCommnad(command):
